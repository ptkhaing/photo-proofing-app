# Copy this to .env for local development, or set the same variable in
# Netlify's Site settings → Environment variables for the live deploy.
# Never commit a real .env file — it's already covered by .gitignore.

VITE_DASHBOARD_PASSCODE=your-passcode-here
